# Task App

A minimal task management app with add, toggle complete, and filter features.

**Requirements:** Python 3.x, Flask

**Install:** `pip install -r requirements.txt`

**Run:** `python3 app.py`

Open http://localhost:5000 in your browser.
